package router

import (
	"github.com/gin-gonic/gin"
	"github.com/wrtgvr/urlshrt/api/handlers"
)

func registerUserRouter(h *handlers.Handler, r *gin.Engine) {
	// group := r.Group("users")
	// {
	// 	group.GET(/:id, h.GetUserHandler)
	// }
}
